代码评价

好的设计
租户安全边界清晰，JWT 只负责识别用户，X-Tenant-ID 只选择工作区，真正授权来自数据库 membership。资源查询同时限定 tenant_id，有效防止越权访问
Refresh Token 设计合理：浏览器仅持有 HttpOnly cookie，数据库保存 hash，并在刷新时撤销旧 session，比长期 JWT 安全
任务历史不可变：重试创建新的 ReviewJob，通过 retry_of_job_id 和 root_job_id 保留完整执行链，而不是覆盖旧任务
队列简单务实，使用 PostgreSQL、FOR UPDATE SKIP LOCKED 和 available_at，不额外引入 Redis/Celery，适合MVP
Mock 与未来真实 LLM 共享结构化 contract，替换模型时不需要修改 HTTP API 和 UI
上传事务失败补偿：数据库提交失败后删除已经写入的文件，认识到了文件系统无法参与数据库事务的问题

坏的设计或需要改进的设计
Worker lease 只有超时，没有所有权隔离：旧 Worker 超时后仍能提交结果，而新 Worker 可能已经接管同一任务
前端异步状态没有归属信息：租户 A 请求返回较慢时，可能在切换到 B 后覆盖 B 的列表。请求结果必须绑定 tenant，或通过 AbortController 取消旧请求
租户一致性主要依靠应用代码：例如 db.get(Document, id)，没有再次限定 tenant；数据库外键也不能保证 ReviewJob.tenant_id 与 Document.tenant_id 一致，建议改为由复合约束兜底
前端组件过于集中：App.tsx 同时负责认证、租户切换、轮询、上传、筛选和报告渲染。建议拆成 session、workspace、history、report 等模块或 hooks